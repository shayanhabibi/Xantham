namespace Xantham.TypeScript.Wire

// <auto-generated>
//   Generated by tools/tsc-ast/generate-ast.mts from microsoft/TypeScript@cf7b9361a33f
//   at tools/scripts/tsc/ast.json.
//   Do not edit by hand - re-run the generator when the pinned schema moves.
// </auto-generated>

/// Named child access, one module per node type.
///
/// These are `Ast.childAtOrder` with the slot filled in, so they are as unchecked as it
/// is: reading `IfStatement.thenStatement` off a node that is not an `IfStatement` returns
/// whatever sits in slot 1 of whatever the node actually is. Call `is` first when the kind
/// is not already known.
[<RequireQualifiedAccess>]
module internal AstNode =

    /// Children of `QualifiedName`.
    [<RequireQualifiedAccess>]
    module QualifiedName =

        /// True when `node` is a `QualifiedName`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.QualifiedName

        /// Slot 0, `left`.
        let left (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.QualifiedName.Left

        /// Slot 1, `right`.
        let right (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.QualifiedName.Right

    /// Children of `ComputedPropertyName`.
    [<RequireQualifiedAccess>]
    module ComputedPropertyName =

        /// True when `node` is a `ComputedPropertyName`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ComputedPropertyName

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ComputedPropertyName.Expression

    /// Children of `Decorator`.
    [<RequireQualifiedAccess>]
    module Decorator =

        /// True when `node` is a `Decorator`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.Decorator

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.Decorator.Expression

    /// Children of `IfStatement`.
    [<RequireQualifiedAccess>]
    module IfStatement =

        /// True when `node` is a `IfStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.IfStatement

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IfStatement.Expression

        /// Slot 1, `thenStatement`.
        let thenStatement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IfStatement.ThenStatement

        /// Slot 2, `elseStatement`. The schema marks it optional.
        let elseStatement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IfStatement.ElseStatement

    /// Children of `DoStatement`.
    [<RequireQualifiedAccess>]
    module DoStatement =

        /// True when `node` is a `DoStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.DoStatement

        /// Slot 0, `statement`.
        let statement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.DoStatement.Statement

        /// Slot 1, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.DoStatement.Expression

    /// Children of `WhileStatement`.
    [<RequireQualifiedAccess>]
    module WhileStatement =

        /// True when `node` is a `WhileStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.WhileStatement

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.WhileStatement.Expression

        /// Slot 1, `statement`.
        let statement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.WhileStatement.Statement

    /// Children of `ForStatement`.
    [<RequireQualifiedAccess>]
    module ForStatement =

        /// True when `node` is a `ForStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ForStatement

        /// Slot 0, `initializer`. The schema marks it optional.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForStatement.Initializer

        /// Slot 1, `condition`. The schema marks it optional.
        let condition (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForStatement.Condition

        /// Slot 2, `incrementor`. The schema marks it optional.
        let incrementor (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForStatement.Incrementor

        /// Slot 3, `statement`.
        let statement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForStatement.Statement

    /// Children of `ForInOrOfStatement`.
    [<RequireQualifiedAccess>]
    module ForInOrOfStatement =

        /// True when `node` is a `ForInOrOfStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            match Ast.kind file node with
            | SyntaxKind.ForInStatement | SyntaxKind.ForOfStatement
                -> true
            | _ -> false

        /// Slot 0, `awaitModifier`. The schema marks it optional.
        let awaitModifier (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForInOrOfStatement.AwaitModifier

        /// Slot 1, `initializer`.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForInOrOfStatement.Initializer

        /// Slot 2, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForInOrOfStatement.Expression

        /// Slot 3, `statement`.
        let statement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ForInOrOfStatement.Statement

    /// Children of `BreakStatement`.
    [<RequireQualifiedAccess>]
    module BreakStatement =

        /// True when `node` is a `BreakStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.BreakStatement

        /// Slot 0, `label`. The schema marks it optional.
        let label (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BreakStatement.Label

    /// Children of `ContinueStatement`.
    [<RequireQualifiedAccess>]
    module ContinueStatement =

        /// True when `node` is a `ContinueStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ContinueStatement

        /// Slot 0, `label`. The schema marks it optional.
        let label (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ContinueStatement.Label

    /// Children of `ReturnStatement`.
    [<RequireQualifiedAccess>]
    module ReturnStatement =

        /// True when `node` is a `ReturnStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ReturnStatement

        /// Slot 0, `expression`. The schema marks it optional.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ReturnStatement.Expression

    /// Children of `WithStatement`.
    [<RequireQualifiedAccess>]
    module WithStatement =

        /// True when `node` is a `WithStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.WithStatement

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.WithStatement.Expression

        /// Slot 1, `statement`.
        let statement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.WithStatement.Statement

    /// Children of `SwitchStatement`.
    [<RequireQualifiedAccess>]
    module SwitchStatement =

        /// True when `node` is a `SwitchStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SwitchStatement

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SwitchStatement.Expression

        /// Slot 1, `caseBlock`.
        let caseBlock (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SwitchStatement.CaseBlock

    /// Children of `CaseBlock`.
    [<RequireQualifiedAccess>]
    module CaseBlock =

        /// True when `node` is a `CaseBlock`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.CaseBlock

        /// The `clauses` list node itself.
        let clausesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CaseBlock.Clauses

        /// The elements of `clauses`, empty when the slot is absent.
        let clauses (file: Ast.SourceFile) (node: int) =
            match clausesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `CaseOrDefaultClause`.
    [<RequireQualifiedAccess>]
    module CaseOrDefaultClause =

        /// True when `node` is a `CaseOrDefaultClause`.
        let is (file: Ast.SourceFile) (node: int) =
            match Ast.kind file node with
            | SyntaxKind.CaseClause | SyntaxKind.DefaultClause
                -> true
            | _ -> false

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CaseOrDefaultClause.Expression

        /// The `statements` list node itself.
        let statementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CaseOrDefaultClause.Statements

        /// The elements of `statements`, empty when the slot is absent.
        let statements (file: Ast.SourceFile) (node: int) =
            match statementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ThrowStatement`.
    [<RequireQualifiedAccess>]
    module ThrowStatement =

        /// True when `node` is a `ThrowStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ThrowStatement

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ThrowStatement.Expression

    /// Children of `TryStatement`.
    [<RequireQualifiedAccess>]
    module TryStatement =

        /// True when `node` is a `TryStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TryStatement

        /// Slot 0, `tryBlock`.
        let tryBlock (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TryStatement.TryBlock

        /// Slot 1, `catchClause`. The schema marks it optional.
        let catchClause (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TryStatement.CatchClause

        /// Slot 2, `finallyBlock`. The schema marks it optional.
        let finallyBlock (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TryStatement.FinallyBlock

    /// Children of `CatchClause`.
    [<RequireQualifiedAccess>]
    module CatchClause =

        /// True when `node` is a `CatchClause`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.CatchClause

        /// Slot 0, `variableDeclaration`. The schema marks it optional.
        let variableDeclaration (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CatchClause.VariableDeclaration

        /// Slot 1, `block`.
        let block (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CatchClause.Block

    /// Children of `LabeledStatement`.
    [<RequireQualifiedAccess>]
    module LabeledStatement =

        /// True when `node` is a `LabeledStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.LabeledStatement

        /// Slot 0, `label`.
        let label (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.LabeledStatement.Label

        /// Slot 1, `statement`.
        let statement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.LabeledStatement.Statement

    /// Children of `ExpressionStatement`.
    [<RequireQualifiedAccess>]
    module ExpressionStatement =

        /// True when `node` is a `ExpressionStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ExpressionStatement

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExpressionStatement.Expression

    /// Children of `Block`.
    [<RequireQualifiedAccess>]
    module Block =

        /// True when `node` is a `Block`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.Block

        /// The `statements` list node itself.
        let statementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.Block.Statements

        /// The elements of `statements`, empty when the slot is absent.
        let statements (file: Ast.SourceFile) (node: int) =
            match statementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `multiLine`, commonData bit 0.
        let multiLine (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `VariableStatement`.
    [<RequireQualifiedAccess>]
    module VariableStatement =

        /// True when `node` is a `VariableStatement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.VariableStatement

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VariableStatement.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `declarationList`.
        let declarationList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VariableStatement.DeclarationList

    /// Children of `VariableDeclaration`.
    [<RequireQualifiedAccess>]
    module VariableDeclaration =

        /// True when `node` is a `VariableDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.VariableDeclaration

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VariableDeclaration.Name

        /// Slot 1, `exclamationToken`. The schema marks it optional.
        let exclamationToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VariableDeclaration.ExclamationToken

        /// Slot 2, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VariableDeclaration.Type

        /// Slot 3, `initializer`. The schema marks it optional.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VariableDeclaration.Initializer

    /// Children of `VariableDeclarationList`.
    [<RequireQualifiedAccess>]
    module VariableDeclarationList =

        /// True when `node` is a `VariableDeclarationList`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.VariableDeclarationList

        /// The `declarations` list node itself.
        let declarationsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VariableDeclarationList.Declarations

        /// The elements of `declarations`, empty when the slot is absent.
        let declarations (file: Ast.SourceFile) (node: int) =
            match declarationsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `BindingPattern`.
    [<RequireQualifiedAccess>]
    module BindingPattern =

        /// True when `node` is a `BindingPattern`.
        let is (file: Ast.SourceFile) (node: int) =
            match Ast.kind file node with
            | SyntaxKind.ObjectBindingPattern | SyntaxKind.ArrayBindingPattern
                -> true
            | _ -> false

        /// The `elements` list node itself.
        let elementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BindingPattern.Elements

        /// The elements of `elements`, empty when the slot is absent.
        let elements (file: Ast.SourceFile) (node: int) =
            match elementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ParameterDeclaration`.
    [<RequireQualifiedAccess>]
    module ParameterDeclaration =

        /// True when `node` is a `ParameterDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.Parameter

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParameterDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `dotDotDotToken`. The schema marks it optional.
        let dotDotDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParameterDeclaration.DotDotDotToken

        /// Slot 2, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParameterDeclaration.Name

        /// Slot 3, `questionToken`. The schema marks it optional.
        let questionToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParameterDeclaration.QuestionToken

        /// Slot 4, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParameterDeclaration.Type

        /// Slot 5, `initializer`. The schema marks it optional.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParameterDeclaration.Initializer

    /// Children of `BindingElement`.
    [<RequireQualifiedAccess>]
    module BindingElement =

        /// True when `node` is a `BindingElement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.BindingElement

        /// Slot 0, `dotDotDotToken`. The schema marks it optional.
        let dotDotDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BindingElement.DotDotDotToken

        /// Slot 1, `propertyName`. The schema marks it optional.
        let propertyName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BindingElement.PropertyName

        /// Slot 2, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BindingElement.Name

        /// Slot 3, `initializer`. The schema marks it optional.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BindingElement.Initializer

    /// Children of `MissingDeclaration`.
    [<RequireQualifiedAccess>]
    module MissingDeclaration =

        /// True when `node` is a `MissingDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.MissingDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MissingDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `FunctionDeclaration`.
    [<RequireQualifiedAccess>]
    module FunctionDeclaration =

        /// True when `node` is a `FunctionDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.FunctionDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `asteriskToken`. The schema marks it optional.
        let asteriskToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionDeclaration.AsteriskToken

        /// Slot 2, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionDeclaration.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 5, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionDeclaration.Type

        /// Slot 6, `body`. The schema marks it optional.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionDeclaration.Body

    /// Children of `ClassDeclaration`.
    [<RequireQualifiedAccess>]
    module ClassDeclaration =

        /// True when `node` is a `ClassDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ClassDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassDeclaration.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `heritageClauses` list node itself. The schema marks it optional.
        let heritageClausesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassDeclaration.HeritageClauses

        /// The elements of `heritageClauses`, empty when the slot is absent.
        let heritageClauses (file: Ast.SourceFile) (node: int) =
            match heritageClausesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `members` list node itself.
        let membersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassDeclaration.Members

        /// The elements of `members`, empty when the slot is absent.
        let members (file: Ast.SourceFile) (node: int) =
            match membersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ClassExpression`.
    [<RequireQualifiedAccess>]
    module ClassExpression =

        /// True when `node` is a `ClassExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ClassExpression

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassExpression.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassExpression.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassExpression.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `heritageClauses` list node itself. The schema marks it optional.
        let heritageClausesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassExpression.HeritageClauses

        /// The elements of `heritageClauses`, empty when the slot is absent.
        let heritageClauses (file: Ast.SourceFile) (node: int) =
            match heritageClausesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `members` list node itself.
        let membersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassExpression.Members

        /// The elements of `members`, empty when the slot is absent.
        let members (file: Ast.SourceFile) (node: int) =
            match membersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `HeritageClause`.
    [<RequireQualifiedAccess>]
    module HeritageClause =

        /// True when `node` is a `HeritageClause`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.HeritageClause

        /// The `types` list node itself.
        let typesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.HeritageClause.Types

        /// The elements of `types`, empty when the slot is absent.
        let types (file: Ast.SourceFile) (node: int) =
            match typesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `token`, commonData bits 0-0.
        let token (file: Ast.SourceFile) (node: int) =
            match Ast.commonData file node &&& 1u with
            | 0u -> ValueSome SyntaxKind.ExtendsKeyword
            | 1u -> ValueSome SyntaxKind.ImplementsKeyword
            | _ -> ValueNone

    /// Children of `InterfaceDeclaration`.
    [<RequireQualifiedAccess>]
    module InterfaceDeclaration =

        /// True when `node` is a `InterfaceDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.InterfaceDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.InterfaceDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.InterfaceDeclaration.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.InterfaceDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `heritageClauses` list node itself. The schema marks it optional.
        let heritageClausesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.InterfaceDeclaration.HeritageClauses

        /// The elements of `heritageClauses`, empty when the slot is absent.
        let heritageClauses (file: Ast.SourceFile) (node: int) =
            match heritageClausesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `members` list node itself.
        let membersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.InterfaceDeclaration.Members

        /// The elements of `members`, empty when the slot is absent.
        let members (file: Ast.SourceFile) (node: int) =
            match membersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `TypeAliasDeclaration`.
    [<RequireQualifiedAccess>]
    module TypeAliasDeclaration =

        /// True when `node` is a `TypeAliasDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            match Ast.kind file node with
            | SyntaxKind.TypeAliasDeclaration | SyntaxKind.JSTypeAliasDeclaration
                -> true
            | _ -> false

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeAliasDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeAliasDeclaration.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeAliasDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 3, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeAliasDeclaration.Type

    /// Children of `EnumMember`.
    [<RequireQualifiedAccess>]
    module EnumMember =

        /// True when `node` is a `EnumMember`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.EnumMember

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.EnumMember.Name

        /// Slot 1, `initializer`. The schema marks it optional.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.EnumMember.Initializer

    /// Children of `EnumDeclaration`.
    [<RequireQualifiedAccess>]
    module EnumDeclaration =

        /// True when `node` is a `EnumDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.EnumDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.EnumDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.EnumDeclaration.Name

        /// The `members` list node itself.
        let membersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.EnumDeclaration.Members

        /// The elements of `members`, empty when the slot is absent.
        let members (file: Ast.SourceFile) (node: int) =
            match membersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ModuleBlock`.
    [<RequireQualifiedAccess>]
    module ModuleBlock =

        /// True when `node` is a `ModuleBlock`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ModuleBlock

        /// The `statements` list node itself.
        let statementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ModuleBlock.Statements

        /// The elements of `statements`, empty when the slot is absent.
        let statements (file: Ast.SourceFile) (node: int) =
            match statementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ImportDeclaration`.
    [<RequireQualifiedAccess>]
    module ImportDeclaration =

        /// True when `node` is a `ImportDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            match Ast.kind file node with
            | SyntaxKind.ImportDeclaration | SyntaxKind.JSImportDeclaration
                -> true
            | _ -> false

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `importClause`. The schema marks it optional.
        let importClause (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportDeclaration.ImportClause

        /// Slot 2, `moduleSpecifier`.
        let moduleSpecifier (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportDeclaration.ModuleSpecifier

        /// Slot 3, `attributes`. The schema marks it optional.
        let attributes (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportDeclaration.Attributes

    /// Children of `ExternalModuleReference`.
    [<RequireQualifiedAccess>]
    module ExternalModuleReference =

        /// True when `node` is a `ExternalModuleReference`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ExternalModuleReference

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExternalModuleReference.Expression

    /// Children of `NamespaceImport`.
    [<RequireQualifiedAccess>]
    module NamespaceImport =

        /// True when `node` is a `NamespaceImport`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NamespaceImport

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamespaceImport.Name

    /// Children of `NamedImports`.
    [<RequireQualifiedAccess>]
    module NamedImports =

        /// True when `node` is a `NamedImports`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NamedImports

        /// The `elements` list node itself.
        let elementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamedImports.Elements

        /// The elements of `elements`, empty when the slot is absent.
        let elements (file: Ast.SourceFile) (node: int) =
            match elementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ExportAssignment`.
    [<RequireQualifiedAccess>]
    module ExportAssignment =

        /// True when `node` is a `ExportAssignment`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ExportAssignment

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportAssignment.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportAssignment.Type

        /// Slot 2, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportAssignment.Expression

        /// `isExportEquals`, commonData bit 0.
        let isExportEquals (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `NamespaceExportDeclaration`.
    [<RequireQualifiedAccess>]
    module NamespaceExportDeclaration =

        /// True when `node` is a `NamespaceExportDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NamespaceExportDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamespaceExportDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamespaceExportDeclaration.Name

    /// Children of `NamespaceExport`.
    [<RequireQualifiedAccess>]
    module NamespaceExport =

        /// True when `node` is a `NamespaceExport`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NamespaceExport

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamespaceExport.Name

    /// Children of `NamedExports`.
    [<RequireQualifiedAccess>]
    module NamedExports =

        /// True when `node` is a `NamedExports`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NamedExports

        /// The `elements` list node itself.
        let elementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamedExports.Elements

        /// The elements of `elements`, empty when the slot is absent.
        let elements (file: Ast.SourceFile) (node: int) =
            match elementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ExportSpecifier`.
    [<RequireQualifiedAccess>]
    module ExportSpecifier =

        /// True when `node` is a `ExportSpecifier`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ExportSpecifier

        /// Slot 0, `propertyName`. The schema marks it optional.
        let propertyName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportSpecifier.PropertyName

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportSpecifier.Name

        /// `isTypeOnly`, commonData bit 0.
        let isTypeOnly (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `CallSignatureDeclaration`.
    [<RequireQualifiedAccess>]
    module CallSignatureDeclaration =

        /// True when `node` is a `CallSignatureDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.CallSignature

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CallSignatureDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CallSignatureDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CallSignatureDeclaration.Type

    /// Children of `ConstructSignatureDeclaration`.
    [<RequireQualifiedAccess>]
    module ConstructSignatureDeclaration =

        /// True when `node` is a `ConstructSignatureDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ConstructSignature

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructSignatureDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructSignatureDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructSignatureDeclaration.Type

    /// Children of `ConstructorDeclaration`.
    [<RequireQualifiedAccess>]
    module ConstructorDeclaration =

        /// True when `node` is a `ConstructorDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.Constructor

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 3, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorDeclaration.Type

        /// Slot 4, `body`. The schema marks it optional.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorDeclaration.Body

    /// Children of `GetAccessorDeclaration`.
    [<RequireQualifiedAccess>]
    module GetAccessorDeclaration =

        /// True when `node` is a `GetAccessorDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.GetAccessor

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.GetAccessorDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.GetAccessorDeclaration.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.GetAccessorDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.GetAccessorDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 4, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.GetAccessorDeclaration.Type

        /// Slot 5, `body`. The schema marks it optional.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.GetAccessorDeclaration.Body

    /// Children of `SetAccessorDeclaration`.
    [<RequireQualifiedAccess>]
    module SetAccessorDeclaration =

        /// True when `node` is a `SetAccessorDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SetAccessor

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SetAccessorDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SetAccessorDeclaration.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SetAccessorDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SetAccessorDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 4, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SetAccessorDeclaration.Type

        /// Slot 5, `body`. The schema marks it optional.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SetAccessorDeclaration.Body

    /// Children of `IndexSignatureDeclaration`.
    [<RequireQualifiedAccess>]
    module IndexSignatureDeclaration =

        /// True when `node` is a `IndexSignatureDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.IndexSignature

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IndexSignatureDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IndexSignatureDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IndexSignatureDeclaration.Type

    /// Children of `MethodSignatureDeclaration`.
    [<RequireQualifiedAccess>]
    module MethodSignatureDeclaration =

        /// True when `node` is a `MethodSignatureDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.MethodSignature

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodSignatureDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodSignatureDeclaration.Name

        /// Slot 2, `postfixToken`. The schema marks it optional.
        let postfixToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodSignatureDeclaration.PostfixToken

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodSignatureDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodSignatureDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 5, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodSignatureDeclaration.Type

    /// Children of `MethodDeclaration`.
    [<RequireQualifiedAccess>]
    module MethodDeclaration =

        /// True when `node` is a `MethodDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.MethodDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `asteriskToken`. The schema marks it optional.
        let asteriskToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.AsteriskToken

        /// Slot 2, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.Name

        /// Slot 3, `postfixToken`. The schema marks it optional.
        let postfixToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.PostfixToken

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 6, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.Type

        /// Slot 7, `body`. The schema marks it optional.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MethodDeclaration.Body

    /// Children of `PropertySignatureDeclaration`.
    [<RequireQualifiedAccess>]
    module PropertySignatureDeclaration =

        /// True when `node` is a `PropertySignatureDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.PropertySignature

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertySignatureDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertySignatureDeclaration.Name

        /// Slot 2, `postfixToken`. The schema marks it optional.
        let postfixToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertySignatureDeclaration.PostfixToken

        /// Slot 3, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertySignatureDeclaration.Type

        /// Slot 4, `initializer`.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertySignatureDeclaration.Initializer

    /// Children of `PropertyDeclaration`.
    [<RequireQualifiedAccess>]
    module PropertyDeclaration =

        /// True when `node` is a `PropertyDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.PropertyDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyDeclaration.Name

        /// Slot 2, `postfixToken`. The schema marks it optional.
        let postfixToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyDeclaration.PostfixToken

        /// Slot 3, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyDeclaration.Type

        /// Slot 4, `initializer`. The schema marks it optional.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyDeclaration.Initializer

    /// Children of `ClassStaticBlockDeclaration`.
    [<RequireQualifiedAccess>]
    module ClassStaticBlockDeclaration =

        /// True when `node` is a `ClassStaticBlockDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ClassStaticBlockDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassStaticBlockDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `body`.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ClassStaticBlockDeclaration.Body

    /// Children of `BinaryExpression`.
    [<RequireQualifiedAccess>]
    module BinaryExpression =

        /// True when `node` is a `BinaryExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.BinaryExpression

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BinaryExpression.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `left`.
        let left (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BinaryExpression.Left

        /// Slot 2, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BinaryExpression.Type

        /// Slot 3, `operatorToken`.
        let operatorToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BinaryExpression.OperatorToken

        /// Slot 4, `right`.
        let right (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.BinaryExpression.Right

    /// Children of `PrefixUnaryExpression`.
    [<RequireQualifiedAccess>]
    module PrefixUnaryExpression =

        /// True when `node` is a `PrefixUnaryExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.PrefixUnaryExpression

        /// Slot 0, `operand`.
        let operand (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PrefixUnaryExpression.Operand

        /// `operator`, commonData bits 0-2.
        let operator (file: Ast.SourceFile) (node: int) =
            match Ast.commonData file node &&& 7u with
            | 0u -> ValueSome SyntaxKind.PlusToken
            | 1u -> ValueSome SyntaxKind.MinusToken
            | 2u -> ValueSome SyntaxKind.TildeToken
            | 3u -> ValueSome SyntaxKind.ExclamationToken
            | 4u -> ValueSome SyntaxKind.PlusPlusToken
            | 5u -> ValueSome SyntaxKind.MinusMinusToken
            | _ -> ValueNone

    /// Children of `PostfixUnaryExpression`.
    [<RequireQualifiedAccess>]
    module PostfixUnaryExpression =

        /// True when `node` is a `PostfixUnaryExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.PostfixUnaryExpression

        /// Slot 0, `operand`.
        let operand (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PostfixUnaryExpression.Operand

        /// `operator`, commonData bits 0-0.
        let operator (file: Ast.SourceFile) (node: int) =
            match Ast.commonData file node &&& 1u with
            | 0u -> ValueSome SyntaxKind.PlusPlusToken
            | 1u -> ValueSome SyntaxKind.MinusMinusToken
            | _ -> ValueNone

    /// Children of `YieldExpression`.
    [<RequireQualifiedAccess>]
    module YieldExpression =

        /// True when `node` is a `YieldExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.YieldExpression

        /// Slot 0, `asteriskToken`. The schema marks it optional.
        let asteriskToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.YieldExpression.AsteriskToken

        /// Slot 1, `expression`. The schema marks it optional.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.YieldExpression.Expression

    /// Children of `ArrowFunction`.
    [<RequireQualifiedAccess>]
    module ArrowFunction =

        /// True when `node` is a `ArrowFunction`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ArrowFunction

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrowFunction.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrowFunction.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrowFunction.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 3, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrowFunction.Type

        /// Slot 4, `equalsGreaterThanToken`.
        let equalsGreaterThanToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrowFunction.EqualsGreaterThanToken

        /// Slot 5, `body`.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrowFunction.Body

    /// Children of `FunctionExpression`.
    [<RequireQualifiedAccess>]
    module FunctionExpression =

        /// True when `node` is a `FunctionExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.FunctionExpression

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionExpression.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `asteriskToken`. The schema marks it optional.
        let asteriskToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionExpression.AsteriskToken

        /// Slot 2, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionExpression.Name

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionExpression.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionExpression.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 5, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionExpression.Type

        /// Slot 6, `body`.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionExpression.Body

    /// Children of `AsExpression`.
    [<RequireQualifiedAccess>]
    module AsExpression =

        /// True when `node` is a `AsExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.AsExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.AsExpression.Expression

        /// Slot 1, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.AsExpression.Type

    /// Children of `SatisfiesExpression`.
    [<RequireQualifiedAccess>]
    module SatisfiesExpression =

        /// True when `node` is a `SatisfiesExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SatisfiesExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SatisfiesExpression.Expression

        /// Slot 1, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SatisfiesExpression.Type

    /// Children of `ConditionalExpression`.
    [<RequireQualifiedAccess>]
    module ConditionalExpression =

        /// True when `node` is a `ConditionalExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ConditionalExpression

        /// Slot 0, `condition`.
        let condition (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalExpression.Condition

        /// Slot 1, `questionToken`.
        let questionToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalExpression.QuestionToken

        /// Slot 2, `whenTrue`.
        let whenTrue (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalExpression.WhenTrue

        /// Slot 3, `colonToken`.
        let colonToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalExpression.ColonToken

        /// Slot 4, `whenFalse`.
        let whenFalse (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalExpression.WhenFalse

    /// Children of `PropertyAccessExpression`.
    [<RequireQualifiedAccess>]
    module PropertyAccessExpression =

        /// True when `node` is a `PropertyAccessExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.PropertyAccessExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAccessExpression.Expression

        /// Slot 1, `questionDotToken`. The schema marks it optional.
        let questionDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAccessExpression.QuestionDotToken

        /// Slot 2, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAccessExpression.Name

    /// Children of `ElementAccessExpression`.
    [<RequireQualifiedAccess>]
    module ElementAccessExpression =

        /// True when `node` is a `ElementAccessExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ElementAccessExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ElementAccessExpression.Expression

        /// Slot 1, `questionDotToken`. The schema marks it optional.
        let questionDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ElementAccessExpression.QuestionDotToken

        /// Slot 2, `argumentExpression`.
        let argumentExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ElementAccessExpression.ArgumentExpression

    /// Children of `CallExpression`.
    [<RequireQualifiedAccess>]
    module CallExpression =

        /// True when `node` is a `CallExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.CallExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CallExpression.Expression

        /// Slot 1, `questionDotToken`. The schema marks it optional.
        let questionDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CallExpression.QuestionDotToken

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CallExpression.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `arguments` list node itself.
        let argumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.CallExpression.Arguments

        /// The elements of `arguments`, empty when the slot is absent.
        let arguments (file: Ast.SourceFile) (node: int) =
            match argumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `NewExpression`.
    [<RequireQualifiedAccess>]
    module NewExpression =

        /// True when `node` is a `NewExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NewExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NewExpression.Expression

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NewExpression.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `arguments` list node itself. The schema marks it optional.
        let argumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NewExpression.Arguments

        /// The elements of `arguments`, empty when the slot is absent.
        let arguments (file: Ast.SourceFile) (node: int) =
            match argumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `MetaProperty`.
    [<RequireQualifiedAccess>]
    module MetaProperty =

        /// True when `node` is a `MetaProperty`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.MetaProperty

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MetaProperty.Name

        /// `keywordToken`, commonData bits 0-0.
        let keywordToken (file: Ast.SourceFile) (node: int) =
            match Ast.commonData file node &&& 1u with
            | 0u -> ValueSome SyntaxKind.ImportKeyword
            | 1u -> ValueSome SyntaxKind.NewKeyword
            | _ -> ValueNone

    /// Children of `NonNullExpression`.
    [<RequireQualifiedAccess>]
    module NonNullExpression =

        /// True when `node` is a `NonNullExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NonNullExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NonNullExpression.Expression

    /// Children of `SpreadElement`.
    [<RequireQualifiedAccess>]
    module SpreadElement =

        /// True when `node` is a `SpreadElement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SpreadElement

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SpreadElement.Expression

    /// Children of `TemplateExpression`.
    [<RequireQualifiedAccess>]
    module TemplateExpression =

        /// True when `node` is a `TemplateExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TemplateExpression

        /// Slot 0, `head`.
        let head (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateExpression.Head

        /// The `templateSpans` list node itself.
        let templateSpansList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateExpression.TemplateSpans

        /// The elements of `templateSpans`, empty when the slot is absent.
        let templateSpans (file: Ast.SourceFile) (node: int) =
            match templateSpansList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `TemplateSpan`.
    [<RequireQualifiedAccess>]
    module TemplateSpan =

        /// True when `node` is a `TemplateSpan`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TemplateSpan

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateSpan.Expression

        /// Slot 1, `literal`.
        let literal (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateSpan.Literal

    /// Children of `TaggedTemplateExpression`.
    [<RequireQualifiedAccess>]
    module TaggedTemplateExpression =

        /// True when `node` is a `TaggedTemplateExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TaggedTemplateExpression

        /// Slot 0, `tag`.
        let tag (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TaggedTemplateExpression.Tag

        /// Slot 1, `questionDotToken`.
        let questionDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TaggedTemplateExpression.QuestionDotToken

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TaggedTemplateExpression.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 3, `template`.
        let template (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TaggedTemplateExpression.Template

    /// Children of `ParenthesizedExpression`.
    [<RequireQualifiedAccess>]
    module ParenthesizedExpression =

        /// True when `node` is a `ParenthesizedExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ParenthesizedExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParenthesizedExpression.Expression

    /// Children of `ArrayLiteralExpression`.
    [<RequireQualifiedAccess>]
    module ArrayLiteralExpression =

        /// True when `node` is a `ArrayLiteralExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ArrayLiteralExpression

        /// The `elements` list node itself.
        let elementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrayLiteralExpression.Elements

        /// The elements of `elements`, empty when the slot is absent.
        let elements (file: Ast.SourceFile) (node: int) =
            match elementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `multiLine`, commonData bit 0.
        let multiLine (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `ObjectLiteralExpression`.
    [<RequireQualifiedAccess>]
    module ObjectLiteralExpression =

        /// True when `node` is a `ObjectLiteralExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ObjectLiteralExpression

        /// The `properties` list node itself.
        let propertiesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ObjectLiteralExpression.Properties

        /// The elements of `properties`, empty when the slot is absent.
        let properties (file: Ast.SourceFile) (node: int) =
            match propertiesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `multiLine`, commonData bit 0.
        let multiLine (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `SpreadAssignment`.
    [<RequireQualifiedAccess>]
    module SpreadAssignment =

        /// True when `node` is a `SpreadAssignment`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SpreadAssignment

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SpreadAssignment.Expression

    /// Children of `PropertyAssignment`.
    [<RequireQualifiedAccess>]
    module PropertyAssignment =

        /// True when `node` is a `PropertyAssignment`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.PropertyAssignment

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAssignment.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAssignment.Name

        /// Slot 2, `postfixToken`. The schema marks it optional.
        let postfixToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAssignment.PostfixToken

        /// Slot 3, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAssignment.Type

        /// Slot 4, `initializer`.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PropertyAssignment.Initializer

    /// Children of `ShorthandPropertyAssignment`.
    [<RequireQualifiedAccess>]
    module ShorthandPropertyAssignment =

        /// True when `node` is a `ShorthandPropertyAssignment`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ShorthandPropertyAssignment

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ShorthandPropertyAssignment.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ShorthandPropertyAssignment.Name

        /// Slot 2, `postfixToken`. The schema marks it optional.
        let postfixToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ShorthandPropertyAssignment.PostfixToken

        /// Slot 3, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ShorthandPropertyAssignment.Type

        /// Slot 4, `equalsToken`. The schema marks it optional.
        let equalsToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ShorthandPropertyAssignment.EqualsToken

        /// Slot 5, `objectAssignmentInitializer`. The schema marks it optional.
        let objectAssignmentInitializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ShorthandPropertyAssignment.ObjectAssignmentInitializer

    /// Children of `DeleteExpression`.
    [<RequireQualifiedAccess>]
    module DeleteExpression =

        /// True when `node` is a `DeleteExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.DeleteExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.DeleteExpression.Expression

    /// Children of `TypeOfExpression`.
    [<RequireQualifiedAccess>]
    module TypeOfExpression =

        /// True when `node` is a `TypeOfExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypeOfExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeOfExpression.Expression

    /// Children of `VoidExpression`.
    [<RequireQualifiedAccess>]
    module VoidExpression =

        /// True when `node` is a `VoidExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.VoidExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.VoidExpression.Expression

    /// Children of `AwaitExpression`.
    [<RequireQualifiedAccess>]
    module AwaitExpression =

        /// True when `node` is a `AwaitExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.AwaitExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.AwaitExpression.Expression

    /// Children of `TypeAssertion`.
    [<RequireQualifiedAccess>]
    module TypeAssertion =

        /// True when `node` is a `TypeAssertion`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypeAssertionExpression

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeAssertion.Type

        /// Slot 1, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeAssertion.Expression

    /// Children of `UnionTypeNode`.
    [<RequireQualifiedAccess>]
    module UnionTypeNode =

        /// True when `node` is a `UnionTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.UnionType

        /// The `types` list node itself.
        let typesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.UnionTypeNode.Types

        /// The elements of `types`, empty when the slot is absent.
        let types (file: Ast.SourceFile) (node: int) =
            match typesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `IntersectionTypeNode`.
    [<RequireQualifiedAccess>]
    module IntersectionTypeNode =

        /// True when `node` is a `IntersectionTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.IntersectionType

        /// The `types` list node itself.
        let typesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IntersectionTypeNode.Types

        /// The elements of `types`, empty when the slot is absent.
        let types (file: Ast.SourceFile) (node: int) =
            match typesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ConditionalTypeNode`.
    [<RequireQualifiedAccess>]
    module ConditionalTypeNode =

        /// True when `node` is a `ConditionalTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ConditionalType

        /// Slot 0, `checkType`.
        let checkType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalTypeNode.CheckType

        /// Slot 1, `extendsType`.
        let extendsType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalTypeNode.ExtendsType

        /// Slot 2, `trueType`.
        let trueType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalTypeNode.TrueType

        /// Slot 3, `falseType`.
        let falseType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConditionalTypeNode.FalseType

    /// Children of `TypeOperatorNode`.
    [<RequireQualifiedAccess>]
    module TypeOperatorNode =

        /// True when `node` is a `TypeOperatorNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypeOperator

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeOperatorNode.Type

        /// `operator`, commonData bits 0-1.
        let operator (file: Ast.SourceFile) (node: int) =
            match Ast.commonData file node &&& 3u with
            | 0u -> ValueSome SyntaxKind.KeyOfKeyword
            | 1u -> ValueSome SyntaxKind.ReadonlyKeyword
            | 2u -> ValueSome SyntaxKind.UniqueKeyword
            | _ -> ValueNone

    /// Children of `InferTypeNode`.
    [<RequireQualifiedAccess>]
    module InferTypeNode =

        /// True when `node` is a `InferTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.InferType

        /// Slot 0, `typeParameter`.
        let typeParameter (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.InferTypeNode.TypeParameter

    /// Children of `ArrayTypeNode`.
    [<RequireQualifiedAccess>]
    module ArrayTypeNode =

        /// True when `node` is a `ArrayTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ArrayType

        /// Slot 0, `elementType`.
        let elementType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ArrayTypeNode.ElementType

    /// Children of `IndexedAccessTypeNode`.
    [<RequireQualifiedAccess>]
    module IndexedAccessTypeNode =

        /// True when `node` is a `IndexedAccessTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.IndexedAccessType

        /// Slot 0, `objectType`.
        let objectType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IndexedAccessTypeNode.ObjectType

        /// Slot 1, `indexType`.
        let indexType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.IndexedAccessTypeNode.IndexType

    /// Children of `TypeReferenceNode`.
    [<RequireQualifiedAccess>]
    module TypeReferenceNode =

        /// True when `node` is a `TypeReferenceNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypeReference

        /// Slot 0, `typeName`.
        let typeName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeReferenceNode.TypeName

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeReferenceNode.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `ExpressionWithTypeArguments`.
    [<RequireQualifiedAccess>]
    module ExpressionWithTypeArguments =

        /// True when `node` is a `ExpressionWithTypeArguments`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ExpressionWithTypeArguments

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExpressionWithTypeArguments.Expression

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExpressionWithTypeArguments.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `LiteralTypeNode`.
    [<RequireQualifiedAccess>]
    module LiteralTypeNode =

        /// True when `node` is a `LiteralTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.LiteralType

        /// Slot 0, `literal`.
        let literal (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.LiteralTypeNode.Literal

    /// Children of `TypePredicateNode`.
    [<RequireQualifiedAccess>]
    module TypePredicateNode =

        /// True when `node` is a `TypePredicateNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypePredicate

        /// Slot 0, `assertsModifier`. The schema marks it optional.
        let assertsModifier (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypePredicateNode.AssertsModifier

        /// Slot 1, `parameterName`.
        let parameterName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypePredicateNode.ParameterName

        /// Slot 2, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypePredicateNode.Type

    /// Children of `ImportAttribute`.
    [<RequireQualifiedAccess>]
    module ImportAttribute =

        /// True when `node` is a `ImportAttribute`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ImportAttribute

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportAttribute.Name

        /// Slot 1, `value`.
        let value (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportAttribute.Value

    /// Children of `ImportAttributes`.
    [<RequireQualifiedAccess>]
    module ImportAttributes =

        /// True when `node` is a `ImportAttributes`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ImportAttributes

        /// The `attributes` list node itself.
        let attributesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportAttributes.Attributes

        /// The elements of `attributes`, empty when the slot is absent.
        let attributes (file: Ast.SourceFile) (node: int) =
            match attributesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `multiLine`, commonData bit 0.
        let multiLine (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

        /// `token`, commonData bits 1-1.
        let token (file: Ast.SourceFile) (node: int) =
            match (Ast.commonData file node >>> 1) &&& 1u with
            | 0u -> ValueSome SyntaxKind.WithKeyword
            | 1u -> ValueSome SyntaxKind.AssertKeyword
            | _ -> ValueNone

    /// Children of `TypeQueryNode`.
    [<RequireQualifiedAccess>]
    module TypeQueryNode =

        /// True when `node` is a `TypeQueryNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypeQuery

        /// Slot 0, `exprName`.
        let exprName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeQueryNode.ExprName

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeQueryNode.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `MappedTypeNode`.
    [<RequireQualifiedAccess>]
    module MappedTypeNode =

        /// True when `node` is a `MappedTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.MappedType

        /// Slot 0, `readonlyToken`. The schema marks it optional.
        let readonlyToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MappedTypeNode.ReadonlyToken

        /// Slot 1, `typeParameter`.
        let typeParameter (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MappedTypeNode.TypeParameter

        /// Slot 2, `nameType`. The schema marks it optional.
        let nameType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MappedTypeNode.NameType

        /// Slot 3, `questionToken`. The schema marks it optional.
        let questionToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MappedTypeNode.QuestionToken

        /// Slot 4, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MappedTypeNode.Type

        /// The `members` list node itself. The schema marks it optional.
        let membersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.MappedTypeNode.Members

        /// The elements of `members`, empty when the slot is absent.
        let members (file: Ast.SourceFile) (node: int) =
            match membersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `TypeLiteralNode`.
    [<RequireQualifiedAccess>]
    module TypeLiteralNode =

        /// True when `node` is a `TypeLiteralNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypeLiteral

        /// The `members` list node itself.
        let membersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeLiteralNode.Members

        /// The elements of `members`, empty when the slot is absent.
        let members (file: Ast.SourceFile) (node: int) =
            match membersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `TupleTypeNode`.
    [<RequireQualifiedAccess>]
    module TupleTypeNode =

        /// True when `node` is a `TupleTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TupleType

        /// The `elements` list node itself.
        let elementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TupleTypeNode.Elements

        /// The elements of `elements`, empty when the slot is absent.
        let elements (file: Ast.SourceFile) (node: int) =
            match elementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `NamedTupleMember`.
    [<RequireQualifiedAccess>]
    module NamedTupleMember =

        /// True when `node` is a `NamedTupleMember`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.NamedTupleMember

        /// Slot 0, `dotDotDotToken`. The schema marks it optional.
        let dotDotDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamedTupleMember.DotDotDotToken

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamedTupleMember.Name

        /// Slot 2, `questionToken`. The schema marks it optional.
        let questionToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamedTupleMember.QuestionToken

        /// Slot 3, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.NamedTupleMember.Type

    /// Children of `OptionalTypeNode`.
    [<RequireQualifiedAccess>]
    module OptionalTypeNode =

        /// True when `node` is a `OptionalTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.OptionalType

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.OptionalTypeNode.Type

    /// Children of `RestTypeNode`.
    [<RequireQualifiedAccess>]
    module RestTypeNode =

        /// True when `node` is a `RestTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.RestType

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.RestTypeNode.Type

    /// Children of `ParenthesizedTypeNode`.
    [<RequireQualifiedAccess>]
    module ParenthesizedTypeNode =

        /// True when `node` is a `ParenthesizedTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ParenthesizedType

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ParenthesizedTypeNode.Type

    /// Children of `FunctionTypeNode`.
    [<RequireQualifiedAccess>]
    module FunctionTypeNode =

        /// True when `node` is a `FunctionTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.FunctionType

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionTypeNode.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionTypeNode.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.FunctionTypeNode.Type

    /// Children of `ConstructorTypeNode`.
    [<RequireQualifiedAccess>]
    module ConstructorTypeNode =

        /// True when `node` is a `ConstructorTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ConstructorType

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorTypeNode.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorTypeNode.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorTypeNode.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 3, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ConstructorTypeNode.Type

    /// Children of `TemplateLiteralTypeNode`.
    [<RequireQualifiedAccess>]
    module TemplateLiteralTypeNode =

        /// True when `node` is a `TemplateLiteralTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TemplateLiteralType

        /// Slot 0, `head`.
        let head (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateLiteralTypeNode.Head

        /// The `templateSpans` list node itself.
        let templateSpansList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateLiteralTypeNode.TemplateSpans

        /// The elements of `templateSpans`, empty when the slot is absent.
        let templateSpans (file: Ast.SourceFile) (node: int) =
            match templateSpansList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `TemplateLiteralTypeSpan`.
    [<RequireQualifiedAccess>]
    module TemplateLiteralTypeSpan =

        /// True when `node` is a `TemplateLiteralTypeSpan`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TemplateLiteralTypeSpan

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateLiteralTypeSpan.Type

        /// Slot 1, `literal`.
        let literal (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TemplateLiteralTypeSpan.Literal

    /// Children of `SyntheticExpression`.
    [<RequireQualifiedAccess>]
    module SyntheticExpression =

        /// True when `node` is a `SyntheticExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SyntheticExpression

        /// Slot 0, `tupleNameSource`. The schema marks it optional.
        let tupleNameSource (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SyntheticExpression.TupleNameSource

    /// Children of `PartiallyEmittedExpression`.
    [<RequireQualifiedAccess>]
    module PartiallyEmittedExpression =

        /// True when `node` is a `PartiallyEmittedExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.PartiallyEmittedExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.PartiallyEmittedExpression.Expression

    /// Children of `JsxElement`.
    [<RequireQualifiedAccess>]
    module JsxElement =

        /// True when `node` is a `JsxElement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxElement

        /// Slot 0, `openingElement`.
        let openingElement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxElement.OpeningElement

        /// The `children` list node itself.
        let childrenList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxElement.Children

        /// The elements of `children`, empty when the slot is absent.
        let children (file: Ast.SourceFile) (node: int) =
            match childrenList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `closingElement`.
        let closingElement (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxElement.ClosingElement

    /// Children of `JsxAttributes`.
    [<RequireQualifiedAccess>]
    module JsxAttributes =

        /// True when `node` is a `JsxAttributes`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxAttributes

        /// The `properties` list node itself.
        let propertiesList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxAttributes.Properties

        /// The elements of `properties`, empty when the slot is absent.
        let properties (file: Ast.SourceFile) (node: int) =
            match propertiesList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JsxNamespacedName`.
    [<RequireQualifiedAccess>]
    module JsxNamespacedName =

        /// True when `node` is a `JsxNamespacedName`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxNamespacedName

        /// Slot 0, `namespace`.
        let ``namespace`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxNamespacedName.Namespace

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxNamespacedName.Name

    /// Children of `JsxOpeningElement`.
    [<RequireQualifiedAccess>]
    module JsxOpeningElement =

        /// True when `node` is a `JsxOpeningElement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxOpeningElement

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxOpeningElement.TagName

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxOpeningElement.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `attributes`.
        let attributes (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxOpeningElement.Attributes

    /// Children of `JsxSelfClosingElement`.
    [<RequireQualifiedAccess>]
    module JsxSelfClosingElement =

        /// True when `node` is a `JsxSelfClosingElement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxSelfClosingElement

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxSelfClosingElement.TagName

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxSelfClosingElement.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `attributes`.
        let attributes (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxSelfClosingElement.Attributes

    /// Children of `JsxFragment`.
    [<RequireQualifiedAccess>]
    module JsxFragment =

        /// True when `node` is a `JsxFragment`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxFragment

        /// Slot 0, `openingFragment`.
        let openingFragment (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxFragment.OpeningFragment

        /// The `children` list node itself.
        let childrenList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxFragment.Children

        /// The elements of `children`, empty when the slot is absent.
        let children (file: Ast.SourceFile) (node: int) =
            match childrenList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `closingFragment`.
        let closingFragment (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxFragment.ClosingFragment

    /// Children of `JsxAttribute`.
    [<RequireQualifiedAccess>]
    module JsxAttribute =

        /// True when `node` is a `JsxAttribute`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxAttribute

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxAttribute.Name

        /// Slot 1, `initializer`. The schema marks it optional.
        let initializer (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxAttribute.Initializer

    /// Children of `JsxSpreadAttribute`.
    [<RequireQualifiedAccess>]
    module JsxSpreadAttribute =

        /// True when `node` is a `JsxSpreadAttribute`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxSpreadAttribute

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxSpreadAttribute.Expression

    /// Children of `JsxClosingElement`.
    [<RequireQualifiedAccess>]
    module JsxClosingElement =

        /// True when `node` is a `JsxClosingElement`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxClosingElement

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxClosingElement.TagName

    /// Children of `JsxExpression`.
    [<RequireQualifiedAccess>]
    module JsxExpression =

        /// True when `node` is a `JsxExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxExpression

        /// Slot 0, `dotDotDotToken`. The schema marks it optional.
        let dotDotDotToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxExpression.DotDotDotToken

        /// Slot 1, `expression`. The schema marks it optional.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JsxExpression.Expression

    /// Data of `JsxText`, which has no children.
    [<RequireQualifiedAccess>]
    module JsxText =

        /// True when `node` is a `JsxText`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JsxText

        /// `containsOnlyTriviaWhiteSpaces`, commonData bit 0.
        let containsOnlyTriviaWhiteSpaces (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `SyntaxList`.
    [<RequireQualifiedAccess>]
    module SyntaxList =

        /// True when `node` is a `SyntaxList`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SyntaxList

        /// The `children` list node itself.
        let childrenList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SyntaxList.Children

        /// The elements of `children`, empty when the slot is absent.
        let children (file: Ast.SourceFile) (node: int) =
            match childrenList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDoc`.
    [<RequireQualifiedAccess>]
    module JSDoc =

        /// True when `node` is a `JSDoc`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDoc

        /// The `comment` list node itself.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDoc.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `tags` list node itself. The schema marks it optional.
        let tagsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDoc.Tags

        /// The elements of `tags`, empty when the slot is absent.
        let tags (file: Ast.SourceFile) (node: int) =
            match tagsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocTypeExpression`.
    [<RequireQualifiedAccess>]
    module JSDocTypeExpression =

        /// True when `node` is a `JSDocTypeExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocTypeExpression

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypeExpression.Type

    /// Children of `JSDocNonNullableType`.
    [<RequireQualifiedAccess>]
    module JSDocNonNullableType =

        /// True when `node` is a `JSDocNonNullableType`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocNonNullableType

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocNonNullableType.Type

    /// Children of `JSDocNullableType`.
    [<RequireQualifiedAccess>]
    module JSDocNullableType =

        /// True when `node` is a `JSDocNullableType`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocNullableType

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocNullableType.Type

    /// Children of `JSDocVariadicType`.
    [<RequireQualifiedAccess>]
    module JSDocVariadicType =

        /// True when `node` is a `JSDocVariadicType`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocVariadicType

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocVariadicType.Type

    /// Children of `JSDocOptionalType`.
    [<RequireQualifiedAccess>]
    module JSDocOptionalType =

        /// True when `node` is a `JSDocOptionalType`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocOptionalType

        /// Slot 0, `type`.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocOptionalType.Type

    /// Children of `JSDocTypeTag`.
    [<RequireQualifiedAccess>]
    module JSDocTypeTag =

        /// True when `node` is a `JSDocTypeTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocTypeTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypeTag.TagName

        /// Slot 1, `typeExpression`.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypeTag.TypeExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypeTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocUnknownTag`.
    [<RequireQualifiedAccess>]
    module JSDocUnknownTag =

        /// True when `node` is a `JSDocUnknownTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocUnknownTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocUnknownTag.TagName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocUnknownTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocTemplateTag`.
    [<RequireQualifiedAccess>]
    module JSDocTemplateTag =

        /// True when `node` is a `JSDocTemplateTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocTemplateTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTemplateTag.TagName

        /// Slot 1, `constraint`.
        let ``constraint`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTemplateTag.Constraint

        /// The `typeParameters` list node itself.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTemplateTag.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTemplateTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocReturnTag`.
    [<RequireQualifiedAccess>]
    module JSDocReturnTag =

        /// True when `node` is a `JSDocReturnTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocReturnTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocReturnTag.TagName

        /// Slot 1, `typeExpression`. The schema marks it optional.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocReturnTag.TypeExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocReturnTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocPublicTag`.
    [<RequireQualifiedAccess>]
    module JSDocPublicTag =

        /// True when `node` is a `JSDocPublicTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocPublicTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocPublicTag.TagName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocPublicTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocPrivateTag`.
    [<RequireQualifiedAccess>]
    module JSDocPrivateTag =

        /// True when `node` is a `JSDocPrivateTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocPrivateTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocPrivateTag.TagName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocPrivateTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocProtectedTag`.
    [<RequireQualifiedAccess>]
    module JSDocProtectedTag =

        /// True when `node` is a `JSDocProtectedTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocProtectedTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocProtectedTag.TagName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocProtectedTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocReadonlyTag`.
    [<RequireQualifiedAccess>]
    module JSDocReadonlyTag =

        /// True when `node` is a `JSDocReadonlyTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocReadonlyTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocReadonlyTag.TagName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocReadonlyTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocOverrideTag`.
    [<RequireQualifiedAccess>]
    module JSDocOverrideTag =

        /// True when `node` is a `JSDocOverrideTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocOverrideTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocOverrideTag.TagName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocOverrideTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocDeprecatedTag`.
    [<RequireQualifiedAccess>]
    module JSDocDeprecatedTag =

        /// True when `node` is a `JSDocDeprecatedTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocDeprecatedTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocDeprecatedTag.TagName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocDeprecatedTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocSeeTag`.
    [<RequireQualifiedAccess>]
    module JSDocSeeTag =

        /// True when `node` is a `JSDocSeeTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocSeeTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSeeTag.TagName

        /// Slot 1, `nameExpression`.
        let nameExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSeeTag.NameExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSeeTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocImplementsTag`.
    [<RequireQualifiedAccess>]
    module JSDocImplementsTag =

        /// True when `node` is a `JSDocImplementsTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocImplementsTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImplementsTag.TagName

        /// Slot 1, `className`.
        let className (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImplementsTag.ClassName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImplementsTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocAugmentsTag`.
    [<RequireQualifiedAccess>]
    module JSDocAugmentsTag =

        /// True when `node` is a `JSDocAugmentsTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocAugmentsTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocAugmentsTag.TagName

        /// Slot 1, `className`.
        let className (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocAugmentsTag.ClassName

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocAugmentsTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocSatisfiesTag`.
    [<RequireQualifiedAccess>]
    module JSDocSatisfiesTag =

        /// True when `node` is a `JSDocSatisfiesTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocSatisfiesTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSatisfiesTag.TagName

        /// Slot 1, `typeExpression`.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSatisfiesTag.TypeExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSatisfiesTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocThrowsTag`.
    [<RequireQualifiedAccess>]
    module JSDocThrowsTag =

        /// True when `node` is a `JSDocThrowsTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocThrowsTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocThrowsTag.TagName

        /// Slot 1, `typeExpression`. The schema marks it optional.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocThrowsTag.TypeExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocThrowsTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocThisTag`.
    [<RequireQualifiedAccess>]
    module JSDocThisTag =

        /// True when `node` is a `JSDocThisTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocThisTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocThisTag.TagName

        /// Slot 1, `typeExpression`.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocThisTag.TypeExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocThisTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocImportTag`.
    [<RequireQualifiedAccess>]
    module JSDocImportTag =

        /// True when `node` is a `JSDocImportTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocImportTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImportTag.TagName

        /// Slot 1, `importClause`. The schema marks it optional.
        let importClause (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImportTag.ImportClause

        /// Slot 2, `moduleSpecifier`.
        let moduleSpecifier (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImportTag.ModuleSpecifier

        /// Slot 3, `attributes`. The schema marks it optional.
        let attributes (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImportTag.Attributes

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocImportTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocCallbackTag`.
    [<RequireQualifiedAccess>]
    module JSDocCallbackTag =

        /// True when `node` is a `JSDocCallbackTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocCallbackTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocCallbackTag.TagName

        /// Slot 1, `typeExpression`.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocCallbackTag.TypeExpression

        /// Slot 2, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocCallbackTag.Name

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocCallbackTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocOverloadTag`.
    [<RequireQualifiedAccess>]
    module JSDocOverloadTag =

        /// True when `node` is a `JSDocOverloadTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocOverloadTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocOverloadTag.TagName

        /// Slot 1, `typeExpression`.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocOverloadTag.TypeExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocOverloadTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocTypedefTag`.
    [<RequireQualifiedAccess>]
    module JSDocTypedefTag =

        /// True when `node` is a `JSDocTypedefTag`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocTypedefTag

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypedefTag.TagName

        /// Slot 1, `typeExpression`. The schema marks it optional.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypedefTag.TypeExpression

        /// Slot 2, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypedefTag.Name

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypedefTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

    /// Children of `JSDocSignature`.
    [<RequireQualifiedAccess>]
    module JSDocSignature =

        /// True when `node` is a `JSDocSignature`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocSignature

        /// The `typeParameters` list node itself. The schema marks it optional.
        let typeParametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSignature.TypeParameters

        /// The elements of `typeParameters`, empty when the slot is absent.
        let typeParameters (file: Ast.SourceFile) (node: int) =
            match typeParametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// The `parameters` list node itself.
        let parametersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSignature.Parameters

        /// The elements of `parameters`, empty when the slot is absent.
        let parameters (file: Ast.SourceFile) (node: int) =
            match parametersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 2, `type`. The schema marks it optional.
        let ``type`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocSignature.Type

    /// Children of `JSDocNameReference`.
    [<RequireQualifiedAccess>]
    module JSDocNameReference =

        /// True when `node` is a `JSDocNameReference`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocNameReference

        /// Slot 0, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocNameReference.Name

    /// Children of `SourceFile`.
    [<RequireQualifiedAccess>]
    module SourceFile =

        /// True when `node` is a `SourceFile`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SourceFile

        /// The `statements` list node itself.
        let statementsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SourceFile.Statements

        /// The elements of `statements`, empty when the slot is absent.
        let statements (file: Ast.SourceFile) (node: int) =
            match statementsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `endOfFileToken`.
        let endOfFileToken (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SourceFile.EndOfFileToken

    /// Children of `ModuleDeclaration`.
    [<RequireQualifiedAccess>]
    module ModuleDeclaration =

        /// True when `node` is a `ModuleDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ModuleDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ModuleDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ModuleDeclaration.Name

        /// Slot 2, `attributes`. The schema marks it optional.
        let attributes (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ModuleDeclaration.Attributes

        /// Slot 3, `body`. The schema marks it optional.
        let body (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ModuleDeclaration.Body

        /// `keyword`, commonData bits 0-0.
        let keyword (file: Ast.SourceFile) (node: int) =
            match Ast.commonData file node &&& 1u with
            | 0u -> ValueSome SyntaxKind.ModuleKeyword
            | 1u -> ValueSome SyntaxKind.NamespaceKeyword
            | _ -> ValueNone

    /// Children of `ImportEqualsDeclaration`.
    [<RequireQualifiedAccess>]
    module ImportEqualsDeclaration =

        /// True when `node` is a `ImportEqualsDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ImportEqualsDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportEqualsDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportEqualsDeclaration.Name

        /// Slot 2, `moduleReference`.
        let moduleReference (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportEqualsDeclaration.ModuleReference

        /// `isTypeOnly`, commonData bit 0.
        let isTypeOnly (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `ExportDeclaration`.
    [<RequireQualifiedAccess>]
    module ExportDeclaration =

        /// True when `node` is a `ExportDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ExportDeclaration

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `exportClause`. The schema marks it optional.
        let exportClause (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportDeclaration.ExportClause

        /// Slot 2, `moduleSpecifier`. The schema marks it optional.
        let moduleSpecifier (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportDeclaration.ModuleSpecifier

        /// Slot 3, `attributes`. The schema marks it optional.
        let attributes (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ExportDeclaration.Attributes

        /// `isTypeOnly`, commonData bit 0.
        let isTypeOnly (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `ImportTypeNode`.
    [<RequireQualifiedAccess>]
    module ImportTypeNode =

        /// True when `node` is a `ImportTypeNode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ImportType

        /// Slot 0, `argument`.
        let argument (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportTypeNode.Argument

        /// Slot 1, `attributes`. The schema marks it optional.
        let attributes (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportTypeNode.Attributes

        /// Slot 2, `qualifier`. The schema marks it optional.
        let qualifier (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportTypeNode.Qualifier

        /// The `typeArguments` list node itself. The schema marks it optional.
        let typeArgumentsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportTypeNode.TypeArguments

        /// The elements of `typeArguments`, empty when the slot is absent.
        let typeArguments (file: Ast.SourceFile) (node: int) =
            match typeArgumentsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `isTypeOf`, commonData bit 0.
        let isTypeOf (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `ImportClause`.
    [<RequireQualifiedAccess>]
    module ImportClause =

        /// True when `node` is a `ImportClause`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ImportClause

        /// Slot 0, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportClause.Name

        /// Slot 1, `namedBindings`. The schema marks it optional.
        let namedBindings (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportClause.NamedBindings

        /// `phaseModifier`, commonData bits 0-1.
        let phaseModifier (file: Ast.SourceFile) (node: int) =
            match Ast.commonData file node &&& 3u with
            | 1u -> ValueSome SyntaxKind.TypeKeyword
            | 2u -> ValueSome SyntaxKind.DeferKeyword
            | _ -> ValueNone

    /// Children of `ImportSpecifier`.
    [<RequireQualifiedAccess>]
    module ImportSpecifier =

        /// True when `node` is a `ImportSpecifier`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.ImportSpecifier

        /// Slot 0, `propertyName`. The schema marks it optional.
        let propertyName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportSpecifier.PropertyName

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.ImportSpecifier.Name

        /// `isTypeOnly`, commonData bit 0.
        let isTypeOnly (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `JSDocLink`.
    [<RequireQualifiedAccess>]
    module JSDocLink =

        /// True when `node` is a `JSDocLink`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocLink

        /// Slot 0, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocLink.Name

    /// Children of `JSDocLinkPlain`.
    [<RequireQualifiedAccess>]
    module JSDocLinkPlain =

        /// True when `node` is a `JSDocLinkPlain`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocLinkPlain

        /// Slot 0, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocLinkPlain.Name

    /// Children of `JSDocLinkCode`.
    [<RequireQualifiedAccess>]
    module JSDocLinkCode =

        /// True when `node` is a `JSDocLinkCode`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocLinkCode

        /// Slot 0, `name`. The schema marks it optional.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocLinkCode.Name

    /// Children of `TypeParameterDeclaration`.
    [<RequireQualifiedAccess>]
    module TypeParameterDeclaration =

        /// True when `node` is a `TypeParameterDeclaration`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.TypeParameter

        /// The `modifiers` list node itself. The schema marks it optional.
        let modifiersList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeParameterDeclaration.Modifiers

        /// The elements of `modifiers`, empty when the slot is absent.
        let modifiers (file: Ast.SourceFile) (node: int) =
            match modifiersList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeParameterDeclaration.Name

        /// Slot 2, `constraint`. The schema marks it optional.
        let ``constraint`` (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeParameterDeclaration.Constraint

        /// Slot 3, `expression`. The schema marks it optional.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeParameterDeclaration.Expression

        /// Slot 4, `defaultType`. The schema marks it optional.
        let defaultType (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.TypeParameterDeclaration.DefaultType

    /// Children of `SyntheticReferenceExpression`.
    [<RequireQualifiedAccess>]
    module SyntheticReferenceExpression =

        /// True when `node` is a `SyntheticReferenceExpression`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.SyntheticReferenceExpression

        /// Slot 0, `expression`.
        let expression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SyntheticReferenceExpression.Expression

        /// Slot 1, `thisArg`.
        let thisArg (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.SyntheticReferenceExpression.ThisArg

    /// Children of `JSDocTypeLiteral`.
    [<RequireQualifiedAccess>]
    module JSDocTypeLiteral =

        /// True when `node` is a `JSDocTypeLiteral`.
        let is (file: Ast.SourceFile) (node: int) =
            Ast.kind file node = SyntaxKind.JSDocTypeLiteral

        /// The `jsdocPropertyTags` list node itself. The schema marks it optional.
        let jsdocPropertyTagsList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocTypeLiteral.JSDocPropertyTags

        /// The elements of `jsdocPropertyTags`, empty when the slot is absent.
        let jsdocPropertyTags (file: Ast.SourceFile) (node: int) =
            match jsdocPropertyTagsList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `isArrayType`, commonData bit 0.
        let isArrayType (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

    /// Children of `JSDocParameterOrPropertyTag`.
    [<RequireQualifiedAccess>]
    module JSDocParameterOrPropertyTag =

        /// True when `node` is a `JSDocParameterOrPropertyTag`.
        let is (file: Ast.SourceFile) (node: int) =
            match Ast.kind file node with
            | SyntaxKind.JSDocParameterTag | SyntaxKind.JSDocPropertyTag
                -> true
            | _ -> false

        /// Slot 0, `tagName`.
        let tagName (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocParameterOrPropertyTag.TagName

        /// Slot 1, `name`.
        let name (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocParameterOrPropertyTag.Name

        /// Slot 2, `typeExpression`. The schema marks it optional.
        let typeExpression (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocParameterOrPropertyTag.TypeExpression

        /// The `comment` list node itself. The schema marks it optional.
        let commentList (file: Ast.SourceFile) (node: int) =
            Ast.childAtOrder file node Slot.JSDocParameterOrPropertyTag.Comment

        /// The elements of `comment`, empty when the slot is absent.
        let comment (file: Ast.SourceFile) (node: int) =
            match commentList file node with
            | ValueSome list -> Ast.children file list
            | ValueNone -> Seq.empty

        /// `isBracketed`, commonData bit 0.
        let isBracketed (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 1u <> 0u

        /// `isNameFirst`, commonData bit 1.
        let isNameFirst (file: Ast.SourceFile) (node: int) =
            Ast.commonData file node &&& 2u <> 0u


