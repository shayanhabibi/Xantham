// <auto-generated>
//   Generated by Xantham.Generator from group-map-lab.
//   Do not edit by hand - regenerate instead.
// </auto-generated>
module rec GroupMapLab

open System
open Fable.Core
open Fable.Core.JsInterop
open Xantham.Fable.Core

/// <summary>
/// A declaration of our own, so a mapped name can be seen passing through a generic.
/// </summary>
[<Interface>]
type Box<'T> =
    abstract value: 'T with get, set
    [<ParamObject; Emit("$0")>]
    static member Create (value: 'T) : Box<'T> = jsNative

/// <summary>
/// Another, to be the argument of a mapped generic.
/// </summary>
[<Interface>]
type Handle =
    abstract id: string with get, set
    [<ParamObject; Emit("$0")>]
    static member Create (id: string) : Handle = jsNative

/// <summary>
/// The destination reads at every position an ordinary reference does.
/// </summary>
[<Interface>]
type Registry =
    /// <summary>
    /// Under an array.
    /// </summary>
    abstract patterns: System.Text.RegularExpressions.Regex[] with get, set
    /// <summary>
    /// Optional, so it arrives wrapped in an option.
    /// </summary>
    abstract held: System.WeakReference<Handle> option with get, set
    /// <summary>
    /// As the argument of a generic this package declares.
    /// </summary>
    abstract boxed: Box<System.Text.RegularExpressions.Regex> with get, set
    [<ParamObject; Emit("$0")>]
    static member Create (patterns: System.Text.RegularExpressions.Regex[], boxed: Box<System.Text.RegularExpressions.Regex>, ?held: System.WeakReference<Handle>) : Registry = jsNative

/// <summary>The package's value exports, each bound to its import.</summary>
[<Erase>]
type Exports =
    /// <summary>
    /// A destination taking no type arguments: a name to write, nothing to apply.
    /// </summary>
    [<Import("compile", "group-map-lab")>]
    static member compile (pattern: System.Text.RegularExpressions.Regex) : System.Text.RegularExpressions.Regex = jsNative
    /// <summary>
    /// A generic destination, applied at the arity the table states.
    /// </summary>
    [<Import("hold", "group-map-lab")>]
    static member hold (handle: Handle) : System.WeakReference<Handle> = jsNative
    /// <summary>
    /// A name the table does not carry. <c>Response</c> and the rest of <c>fetch</c> live in <c>Fable.Fetch</c>,
    /// which this run has no binding for, so the reference widens exactly as it does under the
    /// group's default disposition: mapping is per name, and names outside the table keep what
    /// they had.
    /// </summary>
    [<Import("respond", "group-map-lab")>]
    static member respond () : obj = jsNative
    /// <summary>
    /// A mapped name applied at an arity its destination does not take. A modern lib gives
    /// <c>Iterator</c> a return type and a next type beside its element, and <c>IEnumerator&lt;'T&gt;</c> takes the
    /// element alone, so writing the application would emit F# that does not compile. The reference
    /// widens and the finding names the arity the site applied.
    /// </summary>
    [<Import("walk", "group-map-lab")>]
    static member walk (over: obj) : unit = jsNative
    /// <summary>
    /// The ECMAScript half, from <c>Naming.LibBindings</c>.
    /// </summary>
    [<Import("fetchOne", "group-map-lab")>]
    static member fetchOne (url: string) : JS.Promise<string> = jsNative
    /// <summary>
    /// The DOM half, from the generated <c>Naming.BrowserBindings</c>.
    /// </summary>
    [<Import("handle", "group-map-lab")>]
    static member handle (target: Browser.Types.EventTarget) : unit = jsNative
