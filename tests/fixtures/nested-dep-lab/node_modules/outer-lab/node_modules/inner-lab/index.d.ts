/** A shape installed under a dependency's own `node_modules`, which is where npm puts a
 * version that conflicts with the one the entry package resolved. */
export interface Pulse {
    id: string;
    at: number;
}
