{
  "name": "delegate-name-lab",
  "type": "module",
  "types": "index.d.ts",
  "main": "index.js",
  "description": "A callback the arity rule retains is declared as a named F# delegate carrying the parameter names TypeScript spelled. Two- and three-argument callbacks, a unit-returning one, the same shape at several sites, and a named alias, in every position the corpus writes them."
}
