import { Widget } from "dep-lab";
import { Spare } from "dep_lab";

/** A crate the entry package declares over a dependency's type. */
export interface Cargo {
    widget: Widget;
    spare: Spare;
    label: string;
}

export declare function load(widget: Widget): Cargo;
