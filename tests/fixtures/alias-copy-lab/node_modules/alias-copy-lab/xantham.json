{
    // The dependency is referenced by name (O7). Both halves are registered as entry packages
    // of their own, so the module this templates into is one the gated corpus ships: the F#
    // compiler decides whether every templated name exists, at the arity it is applied at.
    "groups": {
        "alias-copy-dep-lab": "reference"
    }
}
